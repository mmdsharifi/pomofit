"use client"

export function ThemeToggle() {
  // This component now returns null to remove it from the home screen
  // The theme functionality is still available through the useTheme hook
  return null
}
